<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="result" type="com.example.demo.dto.User">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="age" property="age"/>
    </resultMap>
    <!--查询用户信息-->
    <select id="getUserById" resultMap="result">
        select * from user where id=#{id}
    </select>
    <!--删除用户信息-->
    <delete id="delUserById" parameterType="int">
        delete from user where id=#{id}
    </delete>
    <!--查询所有用户信息-->
    <select id="getAllUser" resultMap="result">
        select * from user
    </select>


    <select id="getLogin" resultType="com.example.demo.dto.User">
        select id,password
        from user
        where id=#{id} AND password=#{password}

    </select>

    <!--增加用户信息-->
    <insert id="InsertUser"  parameterType="com.example.demo.dto.User">
        insert into user
        <trim prefix="(" suffix=")"  suffixOverrides=",">
            <if test="id != null" >
                id,
            </if>
            <if test="name != null" >
                name,
            </if>
            <if test="gender != null" >
                gender,
            </if>

            <if test="age != null" >
                age,
            </if>
            <if test="password != null">
                password,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                #{id,jdbcType=INTEGER},
            </if>
            <if test="name != null" >
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="gender != null" >
                #{gender,jdbcType=VARCHAR},
            </if>
            <if test="age != null" >
                #{age,jdbcType=INTEGER},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR}
            </if>
        </trim>
    </insert>

    <!--根据id更改用户信息-->
    <update id="UpdateUser" parameterType="com.example.demo.dto.User">
        update user
        <set >
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="gender != null" >
                gender = #{gender,jdbcType=VARCHAR},
            </if>
            <if test="age != null" >
                age = #{age,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
</mapper>